# ============================================
# AI PROVIDER CONFIGURATION
# ============================================
# Choose your AI provider: 'ollama' | 'openai' | 'huggingface' | 'gemini' | 'anthropic'

AI_PROVIDER= add_your_provider


# OLLAMA (Local Models) - DEFAULT
# No API key needed, runs locally
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2:1b

OLLAMA_EMBEDDING_MODEL=nomic-embed-text


# OPENAI (GPT Models)
# Get API key from: https://platform.openai.com/api-keys
# Free $5 credits for new users
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo

# HUGGING FACE (Free Inference API)
# Get API key from: https://huggingface.co/settings/tokens
# Free: 1000 requests/month
HUGGINGFACE_API_KEY=your_huggingface_api_key_here
HUGGINGFACE_MODEL=microsoft/DialoGPT-medium

# GOOGLE GEMINI (Free Tier)
# Get API key from: https://aistudio.google.com
# Free: 15 requests/minute, 1M tokens/day
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash-exp

# ANTHROPIC CLAUDE
# Get API key from: https://console.anthropic.com
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ANTHROPIC_MODEL=claude-3-haiku-20240307

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ============================================
# RATE LIMITING & SECURITY
# ============================================

# Express Rate Limiting (applies to all /api/* routes)
RATE_LIMIT_WINDOW_MS=900000          # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100          # Max 100 requests per window

# IP-Based Rate Limiting (chat/sections endpoints)
MAX_REQUESTS_PER_IP=12               # Max 12 requests per IP per day
IP_RATE_LIMIT_WINDOW_MS=86400000     # 24 hours (in milliseconds)
MIN_REQUEST_INTERVAL_MS=2000         # Min 2 seconds between requests

# Admin IP Whitelist (comma-separated, bypasses rate limits)
ADMIN_IPS=                           # Example: 192.168.1.100,10.0.0.1

# ============================================
# CACHING
# ============================================

# Response Cache Duration (48 hours for aggressive caching)
CACHE_DURATION_MS=172800000          # 48 hours (172800000 ms)

# Request Timeout
EMBEDDING_REQUEST_TIMEOUT_MS=30000   # 30 seconds
